@layout('layouts/app')
@set('title','News - Register account')

@section('body')
<div class="column is-three-fifths is-offset-one-fifth">
    <h1 class="is-size-4 label">Register Account</h1>
        <form action="{{route('users.register')}}" method="POST">
            {{ csrfField() }}
            <div class="is-flex is-flex-direction-column ">
                <div class="field">
                    <label class="label">Email</label>
                    <div class="control">
                      <input class="input" name="email" type="email" placeholder="john@gmail.com" required>
                    </div>
                </div>
                @if(flashMessages.has('errors.email'))
                    <p> {{ flashMessages.get('errors.email') }} </p>
                @end

                <div class="field">
                    <label class="label">Password</label>
                    <div class="control">
                      <input class="input" name="password" type="password" required>
                    </div>
                </div>
                @if(flashMessages.has('errors.password'))
                    <p> {{ flashMessages.get('errors.password') }} </p>
                @end
                <div class="field is-grouped">
                    <div class="control">
                      <button type="submit" class="button is-light">Register</button>
                    </div>
                    
                  </div>
            </div>
        </form>

</div>
@endsection